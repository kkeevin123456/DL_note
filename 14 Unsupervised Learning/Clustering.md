![[ul (1).jpg]]
因為沒有 label 所以可以採用把靠近的分成一組，來當成新的 classes

---
![[ul (2).jpg]]
K-means: 會先給 k 個點
1.  更新資料，看他們離哪個中心點近
2.  針對每一組，重新算一個中心點
3.  重複1, 2 


缺點: 只能給固定的 k 不能自動決定要分幾組

Hierarchical clustering: 一開始每個點都是一個 class，每次把兩個靠近的點黏起來，我們可以去算每次黏起來，中心點位移量多少 (也就是圖中 distance 的意思)
* 可以決定要幾組 classes
